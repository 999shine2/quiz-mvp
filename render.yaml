services:
  - type: web
    name: quiz-mvp
    runtime: docker
    plan: free
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /var/data
    disk:
      name: quiz_data
      mountPath: /var/data
      sizeGB: 1
